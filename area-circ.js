console.log("Para calcular a área do círculo, informe o tamanho do raio: ");
const raio = 3;
const area = Math.PI * raio ** 2;
const areaStr = area.toFixed(2);
console.log(`A área do círculo é: ${areaStr} m².`);
const areaNum = Number(areaStr);

console.log(areaNum);
